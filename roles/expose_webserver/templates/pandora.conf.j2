server {
        listen 80;
        listen [::]:80;

        server_name {{ server_name }};

        location / {
                proxy_set_header Host $host;
                proxy_pass http://{{ server_name }}:3000;
        }
}
